1) When given a document that describes a journey, go through page by page, and create GET and POST methods for where you interpret it is a form.
2) Give each page the endpoint provided, if one is, else make an appropriate one.
3) Use the information provided per page to create the page, whether that is a title of text to be displayed on the page.
4) When reading a given route file, identify the root router (e.g. `prod.Routes` in HMRC
   projects) and build the full URL from that entry point. Check the project's `application.conf`
   or `routes` file to confirm the root if unsure.
