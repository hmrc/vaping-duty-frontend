Perfect! Thank you for those clarifications. Let me update the plan with the corrected details:

## Updated Implementation Plan

### Key Changes Based on Your Feedback:

1. **No placeholder sections** - Unallocated and Cleared payments tables will be completely omitted for now (not even empty table headers)

2. **Connector return type** - Following the pattern in `GetReturnsConnector`, the connector will return:
   ```scala
   Future[Seq[OutstandingPayment]]
   ```
   (Not `Either[ErrorResponse, ...]` - exceptions will be handled via Future failures)

3. **Display logic** - Take `.headOption` from the Seq and display only the first payment

4. **"You owe" amount** - Display just the single payment amount (from the head of the list)

5. **Empty state** - Show empty table with headers when no payments exist

## Revised Component Details

### Connector Pattern
Following `GetReturnsConnector.scala`:
```scala
class FinancialDataConnector @Inject()(
  config: FrontendAppConfig,
  implicit val httpClient: HttpClientV2
)(using ExecutionContext) extends HttpReadsInstances with Logging {

  def getOutstandingPayments(vpdId: VpdId)(using HeaderCarrier): Future[Seq[OutstandingPayment]] =
    httpClient
      .get(url"${config.getOutstandingPaymentsUrl(vpdId)}")
      .execute[Either[UpstreamErrorResponse, HttpResponse]]
      .recoverWith { case e: Exception =>
        logger.warn(s"Exception while getting outstanding payments: ${e.getMessage}")
        Future.failed(InternalServerException("Failed to get outstanding payments"))
      }
      .flatMap(getResponse)
      .flatMap(parseJson)

  // Similar pattern for getResponse and parseJson as GetReturnsConnector
}
```

### View Logic
```scala
// In the view, handle the optional payment:
@payment match {
  case Some(p) => {
    // Display "You owe" with p.amountDue
    // Display table with single row
  }
  case None => {
    // Display "You have no outstanding payments"
    // Display empty table with headers only
  }
}
```

### View Structure (Simplified)
Based on prototype, but **only** including:
1. "You owe" heading with amount (or "£0" if no payments)
2. "Outstanding payments" section with explanatory text
3. Single table with headers and one row (or empty if no payments)
4. **No** Unallocated payments section
5. **No** Cleared payments section
6. **No** Past payments links

## Updated File List

All files remain the same as in the original plan, with these implementation adjustments:

### Models
- `OutstandingPayment.scala` - Matches backend exactly

### Connector
- Returns `Future[Seq[OutstandingPayment]]`
- Throws exceptions on error (caught by controller)

### Service
- Calls connector
- Returns `Future[Seq[OutstandingPayment]]`
- No additional error wrapping

### Controller
- Calls service
- Takes `.headOption` from result
- On success: passes `Option[OutstandingPayment]` to view
- On failure: catches exception and redirects to `JourneyRecoveryController`

### View Model
- Takes `Option[OutstandingPayment]`
- Calculates display values (formatted amount, date, status tag style)
- Returns "£0" for total if None

### View
- Displays "You owe" with amount from single payment or £0
- Shows table with single row if payment exists
- Shows empty table with headers if no payment
- "Pay now" link has `href="#"`

## Test Coverage

Following the existing connector test pattern (like `EmailVerificationConnectorSpec`):
- Mock `HttpClientV2` and `RequestBuilder`
- Test successful response parsing
- Test invalid JSON response
- Test error status codes
- Test exception handling

## Ready to Implement?

The plan is now complete and aligned with:
- ✅ Existing codebase patterns (`GetReturnsConnector`)
- ✅ Your clarifications (head only, Future return type, no placeholder sections)
- ✅ HMRC standards and project rules
- ✅ Test-driven approach

**When you're ready, please toggle to Act mode and I'll implement this solution!**